# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
www/

# Capacitor
android/app/build/
android/app/release/
android/.gradle/
android/local.properties
android/.idea/
android/build/
ios/App/build/
ios/App/Pods/

# Environment variables (IMPORTANT: Never commit secrets!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# Testing
coverage/
.nyc_output/

# Temporary files
*.tmp
*.temp
.cache/

# OS files
Thumbs.db
.DS_Store

# Misc
*.tsbuildinfo
.eslintcache

# Firebase / Google Services (CRITICAL: Never commit!)
*-firebase-adminsdk-*.json
firebase-service-account.json
google-services.json
google-services_old.json
android/app/google-services.json
android/app/google-services_old.json
nul

# Claude Code local project config
.claude/

# Schema dumps — contain live service_role keys embedded in webhook triggers
prod-schema.sql
prod-schema.*.sql

# Release bundles: archived on disk under releases/ for audit, but never
# committed - each is ~20MB and git would keep every one forever. The audit
# trail is releases/RELEASES.md; Play Console holds the authoritative bundles.
releases/*.aab
releases/*.apk
